import React from 'react';
import PropTypes from 'prop-types';

const ${NAME} = props => {
    return (
        <div>
           ${NAME} has successfully been created. 
        </div>
    );
};

${NAME}.propTypes = {};

${NAME}.defaultProps = {};

export default ${NAME};